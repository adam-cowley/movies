# Neo4j Movie Database Viewer for Python

To install requirements:
```bash
pip install -r requirements
```

To run:
```bash
FLASK_APP=movies.py flask run
```

To view, browse to http://localhost:5000/

